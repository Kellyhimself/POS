import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'POS & Inventory System',
  description: 'A POS and inventory system for multi-store wholesalers',
  manifest: '/manifest.json',
}; 